# 复制本文件为 .env 后填入真实值（.env 已被 .gitignore 排除，不会入库）

# 管理员账户密码。留空则使用本地开发占位密码，且不会覆盖已有账户的密码。
ADMIN_PASSWORD=

# 可选：外部招生数据源（返回 JSON，需含 items 字段）
# ADMISSION_SOURCE=

# 可选：招生数据缓存有效期（天，默认 7）
# ADMISSION_TTL=7

# 可选：真题 / 岗位外部数据源
# EXAMS_SOURCE=
# JOBS_SOURCE=
